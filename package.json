{
  "name": "promise-passthrough",
  "version": "1.0.3",
  "description": "A little function to ensure data flows correctly through the promise chain, while abstracting the logic away from implementation.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "clean": "rm -rf ./dist",
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepublishOnly": "npm run clean; tsc -p ./ --outDir dist/"
  },
  "keywords": [
    "javascript",
    "js",
    "typescript",
    "promise",
    "pass",
    "through",
    "bluebird",
    "es6",
    "functional",
    "state",
    "manage"
  ],
  "author": "Gabriel C. Troia",
  "license": "MIT",
  "dependencies": {
    "es6-promise": "^4.1.1"
  }
}
